using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LightsManager : MonoBehaviour
{
    //todo would be a singleton, would hold reference to player's light. 
    //allows adjusting player light or level's global lights.
    // Start is called before the first frame update
    //day-night etc also would be handled with this
    
}
